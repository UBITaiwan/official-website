# 給 AI Agent 的專案指引

> 反映 2026-09 的專案狀態。改動慣例時請一併更新本文件——見文末「這份文件也要維護」。


這份文件是為了在無人監督的情況下，**保護一批刻意做出來的決定**。
它們看起來都可以「順手改得更好」，但每一條背後都有原因，改掉會退步。

修改前請讀完「不可以做的事」與「改完一定要驗證」兩節。
給人看的操作說明在 [交接手冊.md](交接手冊.md)。

---

## 這是什麼專案

社團法人台灣無條件基本收入協會（UBI Taiwan）的官方網站。
Astro 5 純靜態輸出，**零前端框架依賴**——沒有 React／Vue，這是刻意的，
為了讓非技術的 NGO 團隊能長期維護。請不要引入前端框架。

```bash
npm install
npm run dev      # http://localhost:4321
npm run build    # 產出 dist/
```

---

## 不可以做的事

### 1. 不要重新加回通用卡片與等寬格線

`.card` 與 `.grid--2/3/4` 已於 2026-08 移除，`src/styles/global.css` 裡有註解說明。
理由是協會明確要求「少點卡片或看起來制式的元素」。各區塊現在依內容性質
自訂版型（細線分隔、編號條列、左右交錯、錯位橫列）。

**排版看起來雜亂時，正確做法是為那一區設計版型，不是加回通用卡片。**

### 2. 導覽選單只放「類別」，不放單筆紀錄

`src/components/Header.astro` 開頭有完整說明。簡言之：
改版前「我們在做什麼」有 6 個項目且每年增加 2–3 個，選單長度隨年份線性成長。
現在每個類別底下都有列表頁。

**新增年份（國際交流、年度報告）只加進列表頁的陣列，不要掛進 Header。**

### 3. 不要改動設計系統的色票與間距

`src/styles/tokens.css` 與 `design-system/tokens.json` 必須同步，
`design-system/DESIGN-SYSTEM.md` 是規範。特別注意：

- **warm 橘（`--color-warm-500`）僅用於捐款／報名 CTA**，不要拿來當一般強調色
- **捐款 CTA 全站統一**：`.btn--warm` ＋ 文字「捐款支持」，不要另創「前往捐款支持」等變體
- 一個頁面最多一顆 warm 按鈕

### 4. 不要更動這幾個網址

- `/life-freedom/` —— 生活自由計畫，刻意不搬到 `/experiment/` 底下
- 九個舊網址轉址頁（八個在 `src/pages/` 根層、一個在 `ubi-taiwan/` 底下），
  共用 `src/layouts/RedirectStub.astro`，是舊網址的相容層

### 5. 政策頁的內容範圍已由協會授權放寬

`src/pages/impact/policy.astro` 一度限制「只講問題意識與政策方向」，
2026-08 協會回覆「整份簡報都可以放進去」後已解除。頁首註解記錄了來龍去脈。
**不要因為看到舊註解就把財源內容刪掉。**

---

## 改完一定要驗證

```bash
npm run build
```

然後檢查：

| 檢查 | 為什麼 |
|---|---|
| 建置成功、頁數符合預期 | |
| 站內連結沒有失效 | 含 inline style 的 `url()`，不只 `href`／`src` |
| 每張 `<img>` 都有 `alt` | 無障礙基本要求 |
| 每頁恰好一個 `<h1>` | 轉址頁除外 |
| SVG 圖表有 `aria-label` | 圖表是資訊，不能只有視覺 |
| 表格有 `<caption>` 與 `scope` | 同上 |

**視覺上的改動要實際看過。**跑 `npm run dev` 截圖，不要只說「改好了」。

---

## 搜尋引擎收錄：預設關閉

全站的 `noindex` 與 sitemap 由**同一個旗標**控制，預設都是保守的一邊：

| 建置方式 | 結果 |
|---|---|
| `npm run build` | 全站 noindex、不產生 sitemap |
| `SITE_INDEXABLE=true npm run build` | 開放收錄、產生 sitemap |

這個預設是刻意的：**忘記設定的後果是「沒被收錄」，而不是「意外公開」。**
在正式上線之前不要改動這個預設，也不要把 `SITE_INDEXABLE` 寫死在程式碼裡。

實作在 `src/layouts/BaseLayout.astro` 與 `astro.config.mjs`。
注意 `BaseLayout` 裡用 `String()` 包住環境變數才比較——
Astro 會把 `"true"` 轉成布林值，直接比字串會讓旗標永遠失效。

轉址頁（`src/layouts/RedirectStub.astro`）的 noindex 是寫死的，不受旗標影響——
那些頁面在任何情況下都不該被收錄。

---

## 已知的陷阱

這些都實際浪費過時間。

**環境變數在 `.astro` 裡會被轉型。**
Astro 把 `"true"` 轉成布林值 `true`，所以 `import.meta.env.X === 'true'` 永遠是 false。
要用 `String(import.meta.env.X) === 'true'`。`astro.config.mjs` 用的是 `process.env`，
那邊一律是字串，不受影響——同一個旗標在兩個檔案裡寫法不同是有原因的。

**如果之後部署到子路徑，要重新處理站內絕對路徑。**
本專案曾部署在 GitHub Pages 的子路徑下，需要一支腳本把 `href`／`src`／`url()`
全部加上前綴。該腳本已隨部署設定一併移除。若未來又要部署到子路徑，
記得 inline style 的 `url()` 也要處理——只改 `href`／`src` 不夠，曾經因此上線一批 404 的底圖。

**`.gitignore` 的規則要錨定根目錄。**
曾經寫成 `docs/`，結果連 `public/docs/` 一起忽略，要公開的 PDF 不會進版控，
CI 建置時缺檔、連結上線即 404。現在是 `/docs/`。

**全站 `h3` 有 `text-wrap: balance`。**
它會重新平衡兩行、蓋掉你用 `<span>` 指定的中文斷點。
需要精確斷行時要先關掉（`text-wrap: wrap`）。

**中文沒有詞距，自動換行會斷在詞中間。**
標題要精確斷行就用明確的兩行結構，不要依賴瀏覽器。

**CSS 重啟過渡不能只靠移除／加回 class。**
瀏覽器可能把同一個任務內的兩次變更合併成「沒有變化」。
要先加 `transition: none` 歸零，隔一個 `requestAnimationFrame` 再恢復。
參考 `src/pages/impact/policy.astro` 的 `play()`。

**驗證動畫要看被動畫的值，不是觸發動畫的 class。**
曾用 MutationObserver 確認 class 有切換就宣稱正常，實機卻不動。
要取樣 computed style 的實際值。

**Astro dev server 的樣式表可能是舊的。**
scope id 是路徑雜湊、改內容不會變，瀏覽器認不出要更新。
判斷「壞掉」之前先硬重新整理，或直接驗 `dist/`。

---

## 文字與圖像的原則

**不要用破折號串接句子。**
協會明確回饋「破折號過多會讓人聯想到 AI 生成」。
依語意用「。」「，」「：」，冒號用於引出答案或引言。

**不要憑檔名臆測照片內容。**
`public/images/` 裡有大量檔名無意義的照片。曾把一張證書頒發合照誤標成
國際高峰會。要用一張照片，先打開看過。

**白字壓照片前先算對比。**
不是「看起來夠暗」就好——要取樣文字會落在的區域，最差的像素也要達 4.5:1。
均勻遮罩會把照片壓成一片色塊，用上淡下濃的漸層，文字放在濃的那端。

**不要重畫沒有資料的圖表。**
只有端點值就畫端點，不要憑空補出中間的曲線。
兩個單位不同的指標不要做雙軸，改成同一基準的指數。

---

## 檔案在哪裡

| | |
|---|---|
| 頁面 | `src/pages/` —— 結構見 [README.md](README.md) |
| 共用元件 | `src/components/`（Header、Footer、PageHero） |
| 內容資料 | `src/data/*.json` —— 長文與清單抽出來放這裡 |
| 設計規範 | `design-system/DESIGN-SYSTEM.md` ＋ `tokens.json` |
| 樣式 | `src/styles/tokens.css`（變數）、`global.css`（共用類別） |
| 待辦與交接 | [交接手冊.md](交接手冊.md) |

程式碼裡有 400 多條說明「為什麼」的註解。**動一段程式前先讀它的註解。**
如果註解說了某件事不要做，那是有原因的。

---

## 提交慣例

- **一律開分支，不直接推 `main`**
- commit 訊息說明**為什麼**這樣改，不只說改了什麼
- 開 PR 讓人看過再合併

---

## 這份文件也要維護

**本文件反映 2026-09 的專案狀態。**它描述的規則、陷阱與檔案位置都會隨專案改變，
一旦過時，它就從幫助變成誤導——而下一個 agent 會照著它做。

**改到下面這些東西時，請在同一個 PR 裡更新本文件：**

| 你改了 | 要更新的段落 |
|---|---|
| 設定了部署（CI、Cloudflare 等） | 「改完一定要驗證」＋「搜尋引擎收錄」兩節 |
| 設計系統的色票、間距、按鈕規則 | 「不可以做的事」第 3 項 |
| 導覽結構、頁面網址 | 「不可以做的事」第 2、4 項 |
| 部署方式（例如正式上線、移除子路徑） | 「改完一定要驗證」整段 |
| 踩到新的坑並修好 | 「已知的陷阱」 |
| 移除某條限制，或某項限制已失效 | 對應條目——**刪掉比留著誤導好** |

**如果你發現本文件與程式碼不符**，以程式碼為準，並主動告知對話中的人，
建議他們讓你更新文件。不要默默照著過時的規則做事。

同樣地，[交接手冊.md](交接手冊.md) 裡的「還沒做完的事」與「日期到了要做的事」
需要有人隨進度勾掉。完成某一項之後，請一併把它從清單移除。
