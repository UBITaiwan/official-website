# 給 AI Agent 的專案指引

> 反映 2026-09 的專案狀態。改動慣例時請一併更新本文件——見文末「這份文件也要維護」。


這份文件是為了在無人監督的情況下，**保護一批刻意做出來的決定**。
它們看起來都可以「順手改得更好」，但每一條背後都有原因，改掉會退步。

修改前請讀完「不可以做的事」與「改完一定要驗證」兩節。
給人看的操作說明在 [交接手冊.md](交接手冊.md)。

---

## 這是什麼專案

社團法人台灣無條件基本收入協會（UBI Taiwan）的官方網站。
Astro 5 純靜態輸出，**零前端框架依賴**——沒有 React／Vue，這是刻意的，
為了讓非技術的 NGO 團隊能長期維護。請不要引入前端框架。

```bash
npm install
npm run dev      # http://localhost:4321
npm run build    # 產出 dist/
```

---

## 動手之前：先看 flows/

`flows/` 目錄放的是這個專案的標準作業流程。
**有對應 flow 的工作，一律照 flow 做，不要自己發明步驟。**

| 情況 | 流程 |
|---|---|
| **對方問「我要怎麼開始」** | [flows/怎麼開始.md](flows/怎麼開始.md) —— 先檢查環境，再問這次想做什麼 |
| **任何改動** | [flows/修改流程.md](flows/修改流程.md) —— 每一次都要走 |
| **第一次在這台電腦工作** | [flows/本機預覽與環境檢查.md](flows/本機預覽與環境檢查.md) —— 先確認跑不跑得動 |
| **對方說「我要推上去」** | [flows/推上去.md](flows/推上去.md) —— 檢查有問題要先問過人再修 |
| **新增國際交流／年度報告** | [flows/新增國際交流.md](flows/新增國際交流.md) —— 只進列表頁的陣列，別忘了鄰頁連結 |
| **改壞了、dev 站怪怪的** | [flows/出錯回復.md](flows/出錯回復.md) —— 先判斷壞在哪一層，不要急著修 |
| 其他特定情況 | 見 [flows/README.md](flows/README.md) 的對照表 |

### 收到「我要改 XXX」時的第一個動作

**不是提問，是開分支。**
先 `git fetch` 取得最新的 `main`、開好分支，**然後才回頭確認方向**。

這個順序是刻意的，和一般的直覺相反。理由：

- 「問清楚方向」通常要來回好幾輪，這段期間你手上必須已經有一個安全的位置。
  討論到一半若你逕自動手，改到的是分支，不是 `main`
- 先 fetch 才能保證你讀到的是最新的程式碼，而不是擱置多日的舊版
- 分支開錯、名字取錯都能救（`git branch -m`）；在 `main` 上動手不能

**代價只有一個**：談完發現不用改，把分支丟掉即可，不影響任何事。

例外：對方明說「我只是想了解，先不要改」時不必開分支。

最常被違反的一條：**不要直接在 `main` 上 commit。**
`main` 一有改動就會自動部署到 dev 站，沒有回頭的機會。
一律開分支、開 PR，讓人看過再合併。

---

## 跟協會成員說話的方式

對話的另一端多半**不是工程人員**，而且沒什麼使用 AI agent 的經驗。
他們有能力判斷「這樣做對不對」，但前提是你講的話他們聽得懂。

**動手之前先講你的想法，等對方確認。**不要一收到需求就開始改檔案。
要講四件事：你理解的需求、打算怎麼做、改完會看到的差別、
做不到或需要對方決定的部分。

**用白話文。**檔名、路徑、技術名詞不是不能提，但要放在括號裡當補充，
不要當主詞——對方要判斷的是「這樣做對不對」，不是「你動了哪個檔案」。
對照表見 [flows/修改流程.md](flows/修改流程.md) 第 2 步。

**主動提醒對方可以給素材**（文件、網址、照片），但**不要勉強**。
更不要把「沒有素材」當成不能動工的理由：`public/images/` 裡有大量
還沒用到的真實照片，先自己找過再開口要。

**有多種做法時要給推薦。**列出選項並講清楚差別，然後明講你建議哪一個、
為什麼。把選擇題原封不動丟回去，等於把判斷責任推給最沒有資訊的人。

---

## 不可以做的事

### 1. 不要重新加回通用卡片與等寬格線

`.card` 與 `.grid--2/3/4` 已於 2026-08 移除，`src/styles/global.css` 裡有註解說明。
理由是協會明確要求「少點卡片或看起來制式的元素」。各區塊現在依內容性質
自訂版型（細線分隔、編號條列、左右交錯、錯位橫列）。

**排版看起來雜亂時，正確做法是為那一區設計版型，不是加回通用卡片。**

### 2. 導覽選單只放「類別」，不放單筆紀錄

`src/components/Header.astro` 開頭有完整說明。簡言之：
改版前「我們在做什麼」有 6 個項目且每年增加 2–3 個，選單長度隨年份線性成長。
現在每個類別底下都有列表頁。

**新增年份（國際交流、年度報告）只加進列表頁的陣列，不要掛進 Header。**

### 3. 不要改動設計系統的色票與間距

`src/styles/tokens.css` 與 `design-system/tokens.json` 必須同步，
`design-system/DESIGN-SYSTEM.md` 是規範。特別注意：

- **warm 橘（`--color-warm-500`）僅用於捐款／報名 CTA**，不要拿來當一般強調色
- **捐款 CTA 全站統一**：`.btn--warm` ＋ 文字「捐款支持」，不要另創「前往捐款支持」等變體
- 一個頁面最多一顆 warm 按鈕

### 4. 不要更動這幾個網址

- `/life-freedom/` —— 生活自由計畫，刻意不搬到 `/experiment/` 底下
- 九個舊網址轉址頁（八個在 `src/pages/` 根層、一個在 `ubi-taiwan/` 底下），
  共用 `src/layouts/RedirectStub.astro`，是舊網址的相容層

### 5. 政策頁的內容範圍已由協會授權放寬

`src/pages/impact/policy.astro` 一度限制「只講問題意識與政策方向」，
2026-08 協會回覆「整份簡報都可以放進去」後已解除。頁首註解記錄了來龍去脈。
**不要因為看到舊註解就把財源內容刪掉。**

---

## 改完一定要驗證

```bash
npm run build
npm run check
```

`npm run check` 一次檢查**連結、資安、無障礙**三類問題，並在有問題時以
非零狀態結束。**不要自己另外寫檢查程式**——這支腳本存在的理由，就是
每次手寫檢查邏輯都會重犯同樣的錯（漏掉 URL 編碼的中文路徑、
漏掉 inline style 的 `url()`、只查 `href` 而漏掉 `src`）。

檢查項目與如何判讀輸出見 [flows/推送前檢查.md](flows/推送前檢查.md)。

腳本檢查不到的事仍然要你處理：

- **視覺上的改動要實際看過**——跑 `npm run dev` 截圖，不要只說「改好了」
- **文字壓在照片上要算對比**——取樣文字會落在的區域，最差的像素也要達 4.5:1
- **照片內容要打開看過**——不可憑檔名判斷

### 子路徑檢查（dev 環境會用到）

dev 站部署在 GitHub Pages 的子路徑 `/official-website/` 下，
站內的絕對路徑需要加上前綴。這一步由 CI 自動執行，但你可以在本機先驗：

```bash
PAGES_BASE=/official-website node scripts/ghpages-postbuild.mjs
```

上面是 macOS／Linux 的寫法。**Windows 的 PowerShell 不吃指令前綴的環境變數**，
要改成兩行（這是唯一一條需要分平台的指令，`npm run` 系列都一樣）：

```powershell
$env:PAGES_BASE = "/official-website"; node scripts/ghpages-postbuild.mjs
```

這支腳本在改寫後會**自我檢查**，掃到任何沒加前綴的站內路徑就直接失敗。
這是刻意的——曾經有一批 inline style 的底圖靜靜上線 404。
**如果它報錯，代表你新增了某種它不認得的路徑寫法，要去補規則，不要繞過。**

> 注意：這支腳本會就地改寫 `dist/`，本機驗完請重新 `npm run build` 再繼續開發。

---

## 搜尋引擎收錄：預設關閉

全站的 `noindex` 與 sitemap 由**同一個旗標**控制，預設都是保守的一邊：

| 建置方式 | 結果 |
|---|---|
| `npm run build` | 全站 noindex、不產生 sitemap |
| `SITE_INDEXABLE=true npm run build` | 開放收錄、產生 sitemap |

這個預設是刻意的：**忘記設定的後果是「沒被收錄」，而不是「意外公開」。**
在正式上線之前不要改動這個預設，也不要把 `SITE_INDEXABLE` 寫死在程式碼裡。

實作在 `src/layouts/BaseLayout.astro` 與 `astro.config.mjs`。
注意 `BaseLayout` 裡用 `String()` 包住環境變數才比較——
Astro 會把 `"true"` 轉成布林值，直接比字串會讓旗標永遠失效。

轉址頁（`src/layouts/RedirectStub.astro`）的 noindex 是寫死的，不受旗標影響——
那些頁面在任何情況下都不該被收錄。

---

## 已知的陷阱

這些都實際浪費過時間。

**環境變數在 `.astro` 裡會被轉型。**
Astro 把 `"true"` 轉成布林值 `true`，所以 `import.meta.env.X === 'true'` 永遠是 false。
要用 `String(import.meta.env.X) === 'true'`。`astro.config.mjs` 用的是 `process.env`，
那邊一律是字串，不受影響——同一個旗標在兩個檔案裡寫法不同是有原因的。

**兩個環境的路徑處理不同。**
dev（GitHub Pages）在子路徑 `/official-website/` 下，需要前綴；
正式環境（網域根目錄）不需要。原始碼一律用根路徑撰寫，前綴只在 dev 部署時加。
新增任何非 `href`／`src`／`url()` 的路徑寫法時，要去確認
`scripts/ghpages-postbuild.mjs` 有涵蓋——inline style 的 `url()` 就曾經被漏掉，
在 dev 站上線後才發現一批 404 的底圖。

**`.gitignore` 的規則要錨定根目錄。**
曾經寫成 `docs/`，結果連 `public/docs/` 一起忽略，要公開的 PDF 不會進版控，
CI 建置時缺檔、連結上線即 404。現在是 `/docs/`。

**全站 `h3` 有 `text-wrap: balance`。**
它會重新平衡兩行、蓋掉你用 `<span>` 指定的中文斷點。
需要精確斷行時要先關掉（`text-wrap: wrap`）。

**中文沒有詞距，自動換行會斷在詞中間。**
標題要精確斷行就用明確的兩行結構，不要依賴瀏覽器。

**CSS 重啟過渡不能只靠移除／加回 class。**
瀏覽器可能把同一個任務內的兩次變更合併成「沒有變化」。
要先加 `transition: none` 歸零，隔一個 `requestAnimationFrame` 再恢復。
參考 `src/pages/impact/policy.astro` 的 `play()`。

**驗證動畫要看被動畫的值，不是觸發動畫的 class。**
曾用 MutationObserver 確認 class 有切換就宣稱正常，實機卻不動。
要取樣 computed style 的實際值。

**截圖回傳空白時，先確認是不是預覽面板被收起來了。**
預覽面板隱藏時 `document.visibilityState` 是 `hidden`，此時截圖會是一片白、
CSS 過渡會凍結在中途、lazy 載入的圖不會載入。
**不要據此判斷網站壞了。**改用 DOM 驗證（`read_page`、`find`、實際點擊看落點），
那些不受面板可見性影響。

**Astro dev server 的樣式表可能是舊的。**
scope id 是路徑雜湊、改內容不會變，瀏覽器認不出要更新。
判斷「壞掉」之前先硬重新整理，或直接驗 `dist/`。

---

## 文字與圖像的原則

**不要用破折號串接句子。**
協會明確回饋「破折號過多會讓人聯想到 AI 生成」。
依語意用「。」「，」「：」，冒號用於引出答案或引言。

**不要憑檔名臆測照片內容。**
`public/images/` 裡有大量檔名無意義的照片。曾把一張證書頒發合照誤標成
國際高峰會。要用一張照片，先打開看過。

**白字壓照片前先算對比。**
不是「看起來夠暗」就好——要取樣文字會落在的區域，最差的像素也要達 4.5:1。
均勻遮罩會把照片壓成一片色塊，用上淡下濃的漸層，文字放在濃的那端。

**不要重畫沒有資料的圖表。**
只有端點值就畫端點，不要憑空補出中間的曲線。
兩個單位不同的指標不要做雙軸，改成同一基準的指數。

---

## 檔案在哪裡

| | |
|---|---|
| 頁面 | `src/pages/` —— 結構見 [README.md](README.md) |
| 共用元件 | `src/components/`（Header、Footer、PageHero） |
| 內容資料 | `src/data/*.json` —— 長文與清單抽出來放這裡 |
| 設計規範 | `design-system/DESIGN-SYSTEM.md` ＋ `tokens.json` |
| 樣式 | `src/styles/tokens.css`（變數）、`global.css`（共用類別） |
| 部署 | `.github/workflows/deploy.yml`（dev）、`scripts/ghpages-postbuild.mjs`（子路徑前綴） |
| 待辦與交接 | [交接手冊.md](交接手冊.md) |

程式碼裡有 400 多條說明「為什麼」的註解。**動一段程式前先讀它的註解。**
如果註解說了某件事不要做，那是有原因的。

---

## 提交慣例

完整步驟見 [flows/修改流程.md](flows/修改流程.md)。要點：

- **一律開分支，不直接推 `main`**
- **收到修改請求的第一個動作就是開分支**，不是先討論；理由見上面那一節
- 開分支前先 `git fetch` 取得最新版——現在有多人在同一個 repo 上工作
- commit 訊息說明**為什麼**這樣改，不只說改了什麼
- 開 PR 讓人看過再合併

---

## 這份文件也要維護

**本文件反映 2026-09 的專案狀態。**它描述的規則、陷阱與檔案位置都會隨專案改變，
一旦過時，它就從幫助變成誤導——而下一個 agent 會照著它做。

> **本檔與 `CLAUDE.md` 是同一套規則的兩個入口**：Codex 讀 `AGENTS.md`，
> Claude Code 讀 `CLAUDE.md`。協會成員兩種工具都可能用，因此
> `CLAUDE.md` 內嵌了「第一個動作是開分支」這條最關鍵的規則。
> **改動那條規則時，兩個檔案都要改。**其餘規則的正本一律在本檔。

**改到下面這些東西時，請在同一個 PR 裡更新本文件：**

| 你改了 | 要更新的段落 |
|---|---|
| 設定了部署（CI、Cloudflare 等） | 「改完一定要驗證」＋「搜尋引擎收錄」兩節 |
| 設計系統的色票、間距、按鈕規則 | 「不可以做的事」第 3 項 |
| 導覽結構、頁面網址 | 「不可以做的事」第 2、4 項 |
| 部署方式（例如正式上線、移除子路徑） | 「改完一定要驗證」整段 |
| 踩到新的坑並修好 | 「已知的陷阱」 |
| 移除某條限制，或某項限制已失效 | 對應條目——**刪掉比留著誤導好** |
| `CLAUDE.md` 內嵌的兩條（先開分支、先用白話文講想法） | 本檔＋`CLAUDE.md`＋`flows/修改流程.md` 三處都要改 |

**如果你發現本文件與程式碼不符**，以程式碼為準，並主動告知對話中的人，
建議他們讓你更新文件。不要默默照著過時的規則做事。

同樣地，[交接手冊.md](交接手冊.md) 裡的「還沒做完的事」與「日期到了要做的事」
需要有人隨進度勾掉。完成某一項之後，請一併把它從清單移除。
