# UBI Taiwan 官網 —— 交接手冊

> 反映 2026-09 的狀態。網站會繼續改，文件不會自己跟上——見第九節。


給接手網站的協會成員。**不需要會寫程式**，但需要知道怎麼把事情交代清楚、
以及怎麼確認 AI 做對了。

---

## 一、這個網站是什麼

- 用 Astro 做的**純靜態網站**：沒有資料庫、沒有後台，改內容＝改檔案
- **dev 站（開發環境）**：`https://ubitaiwan.github.io/official-website/`
  合併到 `main` 之後會自動更新，大約兩三分鐘。
  這個站掛著 `noindex`，Google 不會收錄，可以安心放草稿。
- **正式站**：之後會用 Cloudflare 部署到 `ubitaiwan.org`，目前還沒設定。

**重要：dev 站要合併之後才看得到。**

GitHub Pages 一個專案只能有一個站，所以還在 PR 階段的改動不會有獨立網址。
如果想在合併前先看，跟 AI 說：

> 請先檢查我的電腦能不能跑這個網站，可以的話幫我跑起來，我要看 XX 頁

它會先確認你的電腦裝了需要的東西（Mac 和 Windows 都適用），
缺什麼會**先列出來問過你**再裝，然後啟動開發伺服器
（`http://localhost:4321`）並截圖給你。

步驟見 [flows/本機預覽.md](flows/本機預覽.md)。
**不想在自己電腦上裝東西也可以**，那一份裡有替代做法。

**為什麼選這種做法**：沒有後台聽起來麻煩，但也代表沒有東西會被駭、
不用付月費、不會因為外掛更新而壞掉。網站可以放著五年不管也還在。

---

## 二、要改東西時，怎麼跟 AI 說

### 基本原則：說「要什麼結果」，不要說「怎麼改」

| ❌ 不好 | ✅ 好 |
|---|---|
| 「把 pilot.astro 第 120 行的字改掉」 | 「第一階段那頁，見證者引言裡巫彥德的頭銜錯了，應該是 X」 |
| 「加一個卡片」 | 「我想在關於我們那頁介紹三位新理事，你覺得怎麼呈現比較好？」 |
| 「換個顏色」 | 「這個按鈕在手機上看不太清楚」 |

**你負責描述問題，AI 負責找到檔案和決定做法。** 你比它更懂協會要什麼，
它比你更懂程式碼在哪裡。

### 開場先讓它讀規則

第一次對話時，整段複製這句：

> 請先讀 AGENTS.md 和 flows/修改流程.md，然後照那個流程進行。

**這段話對 Claude Code 和 Codex 都適用**，不用管你用的是哪一個。

專案裡有三份東西給 AI 看：`AGENTS.md` 與 `CLAUDE.md` 寫著哪些設計是刻意的、
不能改（前者給 Codex、後者給 Claude Code，內容是同一套）；`flows/` 目錄寫著
標準作業流程。不讓它讀，它很可能「好心」把東西改回制式的樣子，
或是直接在 `main` 上動手。

**每次改動都會走同一套流程**：取得最新版 → 開分支 → 問清楚方向 →
改 → 檢查 → commit → 開 PR 讓你看過 → 合併。
細節見 [flows/修改流程.md](flows/修改流程.md)，你不需要背，AI 會照做。

> **為什麼它還沒問你要改什麼，就先開了分支？**
> 這是刻意的。分支是一個「怎麼改都不會影響正式內容」的工作區，
> 先把它建好，你們才能安心地慢慢討論方向。
> 談完發現不用改，把分支丟掉就好，不會留下痕跡。

### 你手上的東西都可以給它

不用先整理，有什麼給什麼：

| 你有的 | 例子 |
|---|---|
| 文字 | Word 檔、Google 文件、直接貼一段字、會議紀錄 |
| 網址 | 「我看這個網站的做法不錯」、原站的某一頁、新聞連結 |
| 圖檔 | 活動照片、logo、手機截圖（「就是這裡看起來怪怪的」） |

**但沒有也沒關係。**網站現有的照片庫裡還有很多沒用到的素材，
它應該先自己找過，真的不夠才跟你要。
如果它一開口就說「請提供素材」而沒有先找，請它先找。

### 它應該先講想法，你點頭了才動工

**不會直接開始改。**它應該先跟你說四件事：

1. 我理解你要的是……
2. 我打算這樣做……
3. 改完你會看到的差別是……
4. 有什麼做不到、或需要你決定的

**這四件事你都要聽得懂。**如果它講得滿是檔名和英文術語，直接說：

> 請用白話文再講一次，不要提檔名和技術名詞。

**這不是你的問題，是它沒照流程做。**你聽不懂就無法判斷對不對，
照著點頭等於沒看過，那個確認就白費了。

如果它給你兩三個做法要你選，問它「你建議哪一個？為什麼？」。
它應該要有推薦、也說得出取捨；答不出來的話，那些做法它自己也沒想清楚。

### 改完要它自己驗證

改完之後說：

> 請照 AGENTS.md 的「改完一定要驗證」跑一遍，把結果貼給我。

它應該回報：建置成功、連結沒有失效、圖片都有替代文字、子路徑檢查通過。
**如果它只說「改好了」而沒有這些數字，請它補做。**

---

## 三、怎麼確認 AI 做對了

不用看程式碼，看這六件事：

**1. 它有沒有先開分支？**
問它「現在在哪一個分支？」，答案不應該是 `main`。
這是最容易確認、也最要緊的一項。

**2. 它有沒有先講想法，而且你聽得懂？**
動手之前你應該先看到一段「我打算這樣做」，而且是白話文。
你沒點頭它就開始改，或是你根本看不懂它在說什麼，都要叫停。

**3. 它有沒有給你看畫面？**
文字改動可以直接看網站，版面改動應該要有截圖。
「我改好了」但沒有任何證據，就是還沒驗證。

**4. 數字對不對？**
它說「33 個連結都加好了」，你就數數看頁面上是不是 33 個。
說「7 個 logo」就數 7 個。AI 會算錯，但你一數就知道。

**5. 有沒有動到不該動的地方？**
在 GitHub 的 PR 頁面看「Files changed」。如果你只是要改一句話，
卻動到十幾個檔案，要問它為什麼。

**6. 它有沒有誠實說出做不到的部分？**
好的回報會說「這三項做完了，第四項因為缺素材沒做」。
如果每次都「全部完成」，反而要小心。

---

## 四、絕對不要讓 AI 做的事

- **不要讓它直接推上 `main`**。一律開分支、開 PR，你看過再合併。
- **不要讓它「順便整理一下」沒要求的檔案。** 範圍要清楚。
- **不要讓它刪掉看不懂的東西。** 很多奇怪的寫法是為了修某個 bug。
- **不要讓它改 `.gitignore`、`astro.config.mjs`、`scripts/` 底下的東西**，
  除非你明確知道為什麼——這些是部署的基礎設施，壞了整個網站上不去。

如果它建議動這些，請它先解釋原因，你判斷之後再決定。

---

## 五、日期到了要做的事

**網站不會自己更新時間狀態，需要有人提醒它。**

| 時間 | 要做什麼 | 怎麼交代 |
|---|---|---|
| **2026/11/01** | 生活自由計畫進入「計畫啟動」 | 「life-freedom 頁的時程，第 3 項改成已完成、第 4 項改成進行中」 |
| 計畫啟動後 | 加上啟動的照片或說明 | 提供照片，說要放在哪一段 |
| 紀錄片公開時 | 補上觀看連結 | 「實驗計畫頁有兩處預留位，補上這個連結」 |
| 2027 印太高峰會 | 政策方向頁與國際交流頁都已預告，屆時可獨立成頁 | 需要討論怎麼呈現 |

**建議做法**：把上面的日期放進協會的行事曆，到期跳出提醒。
否則網站會停在某個時間點，而沒有人發現。

---

## 六、還沒做完的事

### 需要協會決定

**A5 —— 捐款按鈕的顏色對比不足**
橘底白字的對比只有 2.70:1，無障礙標準要求 4.5:1，視力較弱的人看不清楚。
把橘色加深到 `#B85520` 可以達標，但那會改變全站按鈕的觀感，
屬於品牌決定。**這是目前最該處理的一項。**

**A6 —— 協會簡報第 17 頁的數字有誤**
生育率 1.06 → 0.695 標成「▼18%」，實際算出來是 −34.4%，
第 16 頁標的「▼35%」才對。網站已採用 35%，但**對外簡報建議一併更正**
（那份在 BIEN 多倫多大會發表過）。

**B2 —— 政策方向頁全文審閱**
那是協會第一次在官網正式陳述政策立場，建議完整讀過一遍。

### 需要提供素材

**C5 —— 活動照片的原始檔**
現有照片多數只有 1024px 寬，用在大螢幕的滿版首屏會偏軟。

**D2 —— 紀錄片完整版連結**
公開後告知即可，頁面上有兩處預留位置。

---

## 七、正式上線前要做的事

### 先設定正式環境

dev 站（GitHub Pages）已經在跑了，但正式站還沒有。
正式環境規劃用 Cloudflare 部署到 `ubitaiwan.org`，這部分之後會另外設定。

**設定時務必注意一件事**：只有**正式站**開放搜尋引擎收錄，
dev 站要維持現狀。否則同樣的內容出現在兩個網址上，正式站的排名會被自己拖累。

程式碼**預設就是不開放收錄**，所以 dev 站不用特別處理——
只有正式站需要明確開啟。

### 正式切換到 ubitaiwan.org 時

1. **開放搜尋引擎收錄** —— 在正式環境設定 `SITE_INDEXABLE=true`。
   交代 AI：「要正式上線了，請協助設定開放收錄」。
   **只在正式環境設**，預覽環境要維持預設。
2. **檢查舊網址** —— 有九個轉址頁對應舊網址。
   但那份清單是當初協會提供的，**未必完整**。
   舊站跑了好幾年，外部連結和搜尋結果累積的網址通常更多。
   漏掉的會在切換後變成 404，而且**通常兩三個月後才被發現**。
   交代 AI：「請幫我比對舊站的網址清單和新站，找出會變成 404 的」
3. **確認 sitemap 有產生** —— 跟第 1 項是同一個設定，會一起開啟

**在切換 DNS 之前，先把新站部署起來完整看過一遍。**
切換之後才發現問題，修復期間網站是壞的。

---

## 八、卡住的時候

**網站壞掉了怎麼辦？**
GitHub 上每一次修改都是一個 PR，可以回復。
跟 AI 說：「請把網站回復到上一個正常的版本」。

**AI 一直改不對？**
把它做的改動丟掉，重新開一個對話，把問題描述得更具體。
同一個對話裡繞太久，它會被自己先前的錯誤帶偏。

**不確定該不該答應它的建議？**
問它「這樣改有什麼壞處？」。如果它答不出取捨，那個建議通常不夠成熟。

---

## 九、這些文件會過期，記得請 AI 更新

**本手冊與 `AGENTS.md` 反映 2026-09 的狀態。**網站會繼續改，文件不會自己跟上。
一兩年後如果沒人維護，它們會從幫助變成誤導。

維護方式很簡單，不需要你自己寫：

**做完待辦時**——第六節那五件事，完成一項就跟 AI 說：

> 這一項已經完成了，請把它從交接手冊的待辦清單移除。

**改變做法時**——例如正式上線後不再需要子路徑處理、或設計規則有調整：

> 這次的改動讓 AGENTS.md 裡的某條規則過時了，請一併更新。

**發現文件寫錯時**——AI 說的和文件不符，以**實際程式碼**為準，
並請它更新文件：

> 你說的和 AGENTS.md 寫的不一樣，請確認哪個是對的，然後把文件改成正確的。

**一個好習慣**：每次請 AI 做比較大的改動時，最後加一句
「有需要更新 AGENTS.md 或交接手冊嗎？」。它通常會誠實回答。

---

## 十、相關文件

| 文件 | 給誰看 |
|---|---|
| [AGENTS.md](AGENTS.md) | AI agent（Codex 讀這份）—— 專案規則與陷阱 |
| [CLAUDE.md](CLAUDE.md) | AI agent（Claude Code 讀這份）—— 指向 AGENTS.md |
| [flows/](flows/) | AI agent —— 標準作業流程 |
| [README.md](README.md) | 工程人員 —— 技術結構與資訊架構 |
| [design-system/DESIGN-SYSTEM.md](design-system/DESIGN-SYSTEM.md) | 設計規範 |
| 本文件 | 協會成員 —— 日常操作 |

程式碼裡有 400 多條說明「為什麼這樣做」的註解。
如果 AI 想改某段程式，可以請它**先把那段的註解唸給你聽**，
你就知道當初為什麼那樣寫。
