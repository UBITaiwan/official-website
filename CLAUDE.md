本專案的 AI agent 指引集中在 [AGENTS.md](AGENTS.md)，請先讀完再動手。

> 這份檔案與 `AGENTS.md` 是同一套規則的兩個入口：Claude Code 讀這份，
> Codex 讀 `AGENTS.md`。**規則的正本在 `AGENTS.md`**，改規則請改那邊，
> 並同步下面這三行。

**收到「我要改 XXX」時，第一個動作不是提問，是開分支。**
先取得最新的 `main`、開好分支，再回頭跟對方確認方向。
完整步驟見 [flows/修改流程.md](flows/修改流程.md)。**不要直接在 `main` 上 commit。**

給人看的操作說明在 [交接手冊.md](交接手冊.md)。
